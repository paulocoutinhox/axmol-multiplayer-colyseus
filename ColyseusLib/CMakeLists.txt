# Source code
file(GLOB_RECURSE COLYSEUS_HEADER
    ${CMAKE_CURRENT_LIST_DIR}/**/*.hpp
    ${CMAKE_CURRENT_LIST_DIR}/**/*.h
)

file(GLOB_RECURSE COLYSEUS_SOURCE
    ${CMAKE_CURRENT_LIST_DIR}/**/*.cpp
)

# Propagate the variables to the parent scope
set(GAME_HEADER ${GAME_HEADER} ${COLYSEUS_HEADER} PARENT_SCOPE)
set(GAME_SOURCE ${GAME_SOURCE} ${COLYSEUS_SOURCE} PARENT_SCOPE)

# Include directories
set(GAME_INC_DIRS ${GAME_INC_DIRS}
    ${CMAKE_CURRENT_LIST_DIR}
    PARENT_SCOPE
)
